/**
 *  === server ===
 *  namespace: <%= namespace %>
 *  created at: <%= createdAt %>
 */

export default {

  // 当对应路径的 namespace 链接时，触发 connect
  // 返回当时的 socket 句柄
  connect (socket) {

  },

  // 当对应的 namespace 断开链接时
  // disconnect () {},

  // 当对应的 namespace 正在断开链接时
  // disconnecting () {},

  // 当对应的 namespace 出现错误时
  // error () {}
}